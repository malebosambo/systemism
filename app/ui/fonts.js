import { Courier_Prime, Comfortaa } from 'next/font/google';

export const courier = Courier_Prime({ subsets: ['monospace'] });

export const comfort = Comfortaa({ subsets: ['sans-serif'], weight: ['400'] });